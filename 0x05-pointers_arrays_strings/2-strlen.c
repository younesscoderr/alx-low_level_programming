#include "main.h"

/**
 * _strlen - Entry poin
 * Description: returns the length of a string
 * @s: string to return its length
 * Return: string length
 */

int _strlen(char *s)
{
	return (strlen(s));
}
